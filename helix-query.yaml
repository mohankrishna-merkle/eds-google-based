indices:
  products:
    include:
      - /products/**

    target: /products.json

    properties:
      title:
        select: h1

      category:
        select: meta[name="category"]
        value: content

      description:
        select: meta[name="description"]
        value: content