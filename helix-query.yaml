indices:
  products: &default
    include:
      - /products/**

    target: /products.json

    properties:
      path:
      select: canonical

      title:
        select: meta[name="title"]
        value: content

      category:
        select: meta[name="category"]
        value: content

      description:
        select: meta[name="description"]
        value: content