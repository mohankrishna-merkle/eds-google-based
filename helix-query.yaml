indices:
  products:
    include:
      - /products/**

    target: /products.json

    properties:
      title:
        select: meta[name="title"]
        value: content

      category:
        select: meta[name="category"]
        value: content

      description:
        select: meta[name="description"]
        value: content